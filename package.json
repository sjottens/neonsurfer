{
  "name": "sjottens",
  "version": "1.0.0",
  "description": "Sjottens (Neon Surfer) - a 3D neon surfing runner built with three.js. 100% client-side, no login, no server.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview --port 5174"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^5.3.3",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
